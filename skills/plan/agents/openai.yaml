interface:
  display_name: "Plan"
  short_description: "Turn a specification into ordered tasks"
  default_prompt: "Use $plan to break this specification into verifiable tasks."
policy:
  allow_implicit_invocation: false
