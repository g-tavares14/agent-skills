---
name: test
description: Codex shortcut for the test-driven development lifecycle. Use only when the user explicitly invokes $test.
---

# Test

Read and follow `../test-driven-development/SKILL.md` in full.

Treat `$test` as an explicit request to run its red-green-refactor workflow. For a reported bug, use the canonical skill's Prove-It sequence and verify the regression suite after the fix.
