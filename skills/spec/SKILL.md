---
name: spec
description: Codex shortcut for the specification lifecycle. Use only when the user explicitly invokes $spec.
---

# Spec

Read and follow `../spec-driven-development/SKILL.md` in full.

Treat `$spec` as an explicit request to start that workflow. Produce or update the project specification, then stop at any approval checkpoint required by the canonical skill.
